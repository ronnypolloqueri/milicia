module GranUnidadsHelper
	#Para la vista show
	#Los id deben ser consecutivos
	# def anterior(id)
	# 	if(id == 1)
	# 		return GranUnidad.find(numero_de_registros)
	# 	else
	# 		return GranUnidad.find(id - 1)
	# 	end
	# end

	# def siguiente(id)
	# 	if(id == numero_de_registros)
	# 		return GranUnidad.find(1)
	# 	else
	# 		return GranUnidad.find(id + 1)
	# 	end
	# end

	# def numero_de_registros
	# 	GranUnidad.count
	# end
end
