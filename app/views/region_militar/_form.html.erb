<%= form_for(@region_militar) do |f| %>
  <% if @region_militar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@region_militar.errors.count, "error") %> prohibited this region_militar from being saved:</h2>

      <ul>
      <% @region_militar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :himno %><br>
    <%= f.text_area :himno %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
