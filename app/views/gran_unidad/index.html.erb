<h1>Indice de Grandes Unidades</h1>

<table class="table table-responsive table-striped">
  <thead>
    <tr>
      <th>Región militar</th>
      <th>Nombre</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @gran_unidad.each do |gran_unidad| %>
      <tr>
        <td><%= gran_unidad[2] %></td>
        <td><%= link_to gran_unidad[1], gran_unidad_path(gran_unidad[0]) %></td>
        <td><%= link_to 'Editar', edit_gran_unidad_path(gran_unidad[0]) %></td>
        <td><%= link_to 'Eliminar', gran_unidad_path(gran_unidad[0]), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Gran unidad', new_gran_unidad_path %>
